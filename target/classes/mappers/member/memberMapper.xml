<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<insert id="signupMember" parameterType="com.spmall.member.MemberVO">
		<![CDATA[
			insert into `member`(member_id, member_pwd, member_name, member_gender, member_phone, member_email1, member_eamil2, member_addr1, member_addr2, member_addr3, member_addr4)
			values(	#{member_id},
					#{member_pwd},
					#{member_name},
					#{member_gender},
					#{member_phone},
					#{member_email1},
					#{member_email2},
					#{member_addr1},
					#{member_addr2},
					#{member_addr3},
					#{member_addr4}
			)
		]]>
	</insert>
	
	<select id = "idoverlap" parameterType="String" resultType ="int">
		select Count(*)
		from member
		where member_id = #{member_id};
	</select>
</mapper>